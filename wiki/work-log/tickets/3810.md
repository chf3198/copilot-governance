---
title: "#3810 Stop hook: gate uncommitted-block on session-attributable code, not baseline drift"
type: work-log
content_trust_score: 0.9
created: "2026-07-15"
updated: "2026-07-15"
tags: [issue, wiki-b, hooks, stop-hook, baseline-drift, false-positive]
related: [[ticket-3809-goal-lens-free-first-tracked]]
status: CLOSED
source_path: "github:issue/3810"
generated_by_run: local
---
# #3810 — Stop hook: session-attributable uncommitted gate (fix baseline-drift false positive)

> **Source**: github:issue/3810 | **state: CLOSED (resolution: released)** | **Labels**: type:bug, priority:P2, area:hooks, status:done, accountable-team:claude-code
> Operator directive 2026-07-15. Relates: #2005 (same bug class), #3054 (baseline_drift_override), #3801 (baseline capture).

## Body

The `Stop` hook false-positive-blocks session end (`"Stop blocked: uncommitted changes; Admin
baton incomplete."`) on the parked `feat/3026` canonical checkout, whose standing multi-ticket
baseline drift (~40 modified + ~724 untracked) is intentionally uncommitted (#3801, re-park pending).

Root cause: `check_uncommitted` blocks on ANY uncommitted code file once `roles['collaborator']`
is True, with no attribution to whether THIS session authored it, and no SessionStart baseline
snapshot exists. Same disease already cured for `detect_session_signals` under #2005.

Fix: block only on **session-attributable** uncommitted code via a SessionStart baseline-delta
snapshot in `governance_state`, plus `baseline_drift_override` parity in `check_uncommitted`;
fail-safe to legacy block when snapshot missing. Full scope + 7 ACs in
`wiki/work-log/ticket-3810/manager-scope.md`.

## Acceptance criteria
- [x] AC1 baseline == standing drift → Stop does NOT block on pre-existing files
- [x] AC2 new post-SessionStart uncommitted code file → STILL blocks (no weakening)
- [x] AC3 baseline_drift_override honored by check_uncommitted (parity with check_admin_ops #3054)
- [x] AC4 SessionStart records baseline into governance_state; branch-change resets; missing→legacy block
- [x] AC5 sibling spec + harness-self-test-registry entry (#1893); unit tests AC1-AC4; py_compile clean
- [x] AC6 required CI green; cross-family $0 consensus PASS (receipt recorded)
- [x] AC7 no change to unrelated Stop conditions (client-arbitration/internal-conflict/admin-ops/wiki)

## Resolution — DONE (released) 2026-07-15
Merged PR #33 (squash `aaf60bb`). New stdlib-only `hooks/scripts/session_baseline.py` +
SessionStart snapshot (`session_context.py`) + delta gate (`stop_checks.check_uncommitted` via
`should_block`) + wiring (`stop_reminder.py`). Fail-safe: unresolved baseline ⇒ legacy block
(never fail-open). Cross-family $0 consensus PASS receipt `da0aac6aeceb212b`. All 10 CI checks
green. Live end-to-end verified on the real 764-path parked checkout (AC1 no false block, AC2
fresh file still blocks, AC3 override suppresses). Full baton evidence:
`ticket-3810/collaborator-validation.md`, `admin-handoff.md`, `consultant-closeout.md`.

**Follow-up (out of scope, AC7):** `stop_reminder`'s separate `worktree-drift` catch-all
(`classify_internal_conflict`) also blocks any dirty tree; full parked-checkout unblock needs a
sibling ticket to make it session-attributable. Flagged.

Signed-by: claude-code
Team&Model: claude-code:claude-opus-4-8
Role: consultant
cross_family_receipt: da0aac6aeceb212b
verification-timestamp: 2026-07-15T16:45:00Z
