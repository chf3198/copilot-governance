### Added
- Epic #1948: unit test suite for the objective test-floor classifier (`scripts/test-floor-classifier.spec.js`, 30 cases) — the test-sufficiency enforcer is now itself covered.
- Epic #1948 P1.2: advisory (non-blocking) `test-floor` reconcile emit wired into `collaborator-preflight`, surfacing declared-vs-derived floor gaps before a COLLABORATOR_HANDOFF. Honors the `TEST_FLOOR_DISABLED` rollback flag; promotion to a blocking gate remains replay-eval-gated (#1771/#1875) and deferred by design.

### Changed
- Epic #1948 reconciled from stale `triage` to its true implemented state (advisory-complete); `EPIC_RESCOPE` recorded with a $0 cross-family consensus receipt in lieu of operator sign-off.
